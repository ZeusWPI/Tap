<tr>
  <td class="order_date">
    <%= order.created_at.strftime("%d %b %Y at %H:%M") %>
  </td>
</tr>
<tr>
  <td>
    <%= order.to_sentence %>
  </td>
  <td>
    <% p order %>
    <%= euro_from_cents(order.price_cents) %>
  </td>
</tr>
